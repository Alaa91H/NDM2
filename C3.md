# C3 — التقرير الهندسي الكامل

**التاريخ:** 2026-07-29  
**النطاق:** `D:\Nova\NOVA\src-tauri\src\daemon\` — 48 ملف، ~15,000 سطر  
**فريق المراجعة:** Systems Engineer, Rust Engineer, Network Expert, Performance Engineer, Memory Safety Auditor, Concurrency Specialist, Software Architect, Static Analysis Expert

---

## جدول المحتويات
1. [ملخص تنفيذي](#1-ملخص-تنفيذي)
2. [خريطة تدفق التنفيذ](#2-خريطة-تدفق-التنفيذ)
3. [خريطة تدفق التحميل](#3-خريطة-تدفق-التحميل)
4. [خريطة الاعتماد بين الوحدات](#4-خريطة-الاعتماد-بين-الوحدات)
5. [جميع الأخطاء — مرتبة حسب الخطورة](#5-جميع-الأخطاء--مرتبة-حسب-الخطورة)
6. [الأكواد الميتة](#6-الأكواد-الميتة)
7. [مشاكل التزامن](#7-مشاكل-التزامن)
8. [مشاكل libcurl](#8-مشاكل-libcurl)
9. [مشاكل البنية](#9-مشاكل-البنية)
10. [نقاط ضعف الأداء](#10-نقاط-ضعف-الأداء)
11. [مشاكل أمنية](#11-مشاكل-أمنية)
12. [خطة الإصلاح](#12-خطة-الإصلاح)

---

## 1. ملخص تنفيذي

تم فحص 48 ملف بمجموع ~15,000 سطر كود Rust. النتائج:

| الفئة | العدد | أقصى خطورة |
|-------|-------|------------|
| **أخطاء حرجة (CRITICAL)** | 2 | تعطل كامل لاستقصاء التوقف، وحقن وسائط في curl |
| **عالية (HIGH)** | 12 | سباق بيانات، تعارض إيقاف، تسريب عمليات، SSRF، كود ميت |
| **متوسطة (MEDIUM)** | 28 | تسريب أقفال، أخطاء منطقية، نقص تحقق، مشاكل أداء |
| **منخفضة (LOW)** | 34 | تحسينات، توثيق، هامشية |
| **كود ميت** | ~1,200 سطر | 3 وحدات كاملة + أجزاء من وحدات أخرى |

إجمالي الأخطاء المؤكدة: **76** (2 CRITICAL + 12 HIGH + 28 MEDIUM + 34 LOW)

**الخلاصة:** الكود جيد التنظيم والهندسة لكن يعاني من:
1. استقصاء التوقف (stall detection) لا يعمل أبداً في مسار التحميل الفردي
2. ثغرة حقن وسائط curl عبر `rawOptions`
3. نظاما تكيف متوازيان (AdaptiveEngine + AdaptiveConnectionManager)
4. وحدات كاملة غير مستخدمة (policy_engine, self_healing, disk_writer, plugin_api)
5. لا توثيق لترتيب الأقفال — خطر deadlock كامن
6. DNS rebinding غير محمي — SSRF ممكن
7. أخطاء توافق RFC 6266 في محلل Content-Disposition

---

## 2. خريطة تدفق التنفيذ

```
main.rs
  └─ install_panic_hook()
  └─ run() / run_integration_mode()
       └─ lib.rs: nova_builder()
            ├─ find_available_daemon_port()     ← حلقة unbounded
            ├─ start_daemon()                   ← يبدأ thread + tokio runtime
            │    ├─ DaemonState::new()
            │    │    ├─ EventBus::new()
            │    │    ├─ BandwidthManager::new()
            │    │    ├─ MetadataCache::new()
            │    │    ├─ DownloadRuleEngine::new()
            │    │    ├─ ProfileManager::new()
            │    │    ├─ ResourceManager::new()
            │    │    ├─ DieOrchestrator::new()
            │    │    ├─ ExtractorRegistry::new()
            │    │    ├─ CurlMultiGuard::new()
            │    │    ├─ AdaptiveConnectionManager
            │    │    ├─ DynamicSegmentScheduler
            │    │    └─ RetryState
            │    ├─ external_tools.discover()    ← blocking inside runtime! ⚠️
            │    ├─ tokio::spawn (scheduler)
            │    ├─ start_persistence_loop()     ← async task
            │    ├─ start_telegram_bot()         ← async task
            │    ├─ axum::serve(listener, app)   ← main HTTP server
            │    │    ├─ POST /v1/add           → create_curl_task()
            │    │    ├─ POST /v1/pause         → pause_task()
            │    │    ├─ POST /v1/resume        → resume_task()
            │    │    ├─ POST /v1/cancel        → cancel_task()
            │    │    ├─ POST /v1/remove        → delete_task()
            │    │    ├─ POST /v1/rename        → rename_destination_on_disk()
            │    │    ├─ POST /v1/edit          → update_task_metadata()
            │    │    ├─ GET  /v1/tasks         → list_all_tasks()
            │    │    ├─ GET  /v1/events        → handle_v1_events() [SSE]
            │    │    ├─ POST /v1/analyze       → handle_v1_analyze()
            │    │    ├─ POST /v1/analyze/progress → handle_v1_analyze_progress() [SSE]
            │    │    ├─ POST /v1/resolve       → handle_v1_stream_resolve()
            │    │    ├─ GET/POST /engine/...   → engine routes
            │    │    └─ ...
            │    └─ shutdown_signal (ctrl_c + EXTERNAL_SHUTDOWN)
            │         └─ save_now()
            └─ tauri::Builder
                 ├─ setup (starts daemon thread)
                 ├─ commands (invoke_handler)
                 └─ run()
```

### مسار إنشاء التحميل (CURL)

```
create_curl_task() [task_api.rs]
  ├─ destination_from_body()        ← يحدد مسار الحفظ
  ├─ FileWriter::ensure_parent()    ← ينشئ المجلدات
  ├─ build_curl_args_with_capabilities()  ← يبني وسائط curl
  ├─ plan_from_job()                ← يخطط التحميل
  │    ├─ parse_config()            ← يحلل إعدادات المستخدم
  │    ├─ DirectUrl::parse()        ← يتحقق من URL
  │    └─ split_ranges()            ← يقسم إلى segments
  └─ start_curl_process()
       ├─ spawn worker thread       ← run_libcurl_download
       │    ├─ run_single_libcurl() OR run_segmented_libcurl()
       │    │    ├─ create_easy_for_range_ext()  ← ينشئ Easy2 لكل segment
       │    │    ├─ apply_easy_options()          ← يطبق إعدادات curl
       │    │    ├─ CurlMultiGuard::new()
       │    │    ├─ configure_limits()
       │    │    ├─ attach_socket_runtime()
       │    │    ├─ drive_multi_socket() OR drive_multi_wait_perform()
       │    │    │    ├─ multi.perform()
       │    │    │    ├─ multi.wait() / multi.action()
       │    │    │    └─ check_multi_messages()
       │    │    ├─ response code handling (200,206,304,412,416)
       │    │    └─ merge_parts() (للـ segmented)
       │    ├─ retry loop (حتى 24 ساعة wall time)
       │    │    ├─ self_healer.on_failure()     ← never called! ⚠️
       │    │    ├─ mirror failover
       │    │    ├─ segmented → single fallback
       │    │    └─ exponential backoff + jitter
       │    └─ watchdog thread       ← يتحقق من stall كل 3 ثوانٍ
       └─ struct CurlJob (id, task, cancel_token, run_generation)
```

---

## 3. خريطة تدفق التحميل

```
[POST /v1/add]
     │
     ▼
destination_from_body() ─── save_path بدون تحقق traversal ⚠️
     │
     ▼
build_curl_args_with_capabilities()
     │  └── rawOptions → apply_raw_curl_options() ← INJECTION ⚠️⚠️
     ▼
plan_from_job()
     │  ├── parse_config() → CurlTransferConfig
     │  ├── DirectUrl::parse() → allowlist بروتوكولات
     │  └── split_ranges() → SegmentRange[]
     ▼
start_curl_process()
     │
     ├── Worker Thread ───────────────────────────
     │   │
     │   ├── resolve_effective_target()
     │   │    ├── RIE preflight skip (إذا موجود)
     │   │    ├── Known extension fast path (assumes range=true)
     │   │    └── Full preflight HEAD/GET
     │   │
     │   ├── run_single_libcurl() / run_segmented_libcurl()
     │   │    │
     │   │    ├── create_easy_for_range_ext()
     │   │    │    ├── OpenOptions::create(true).append(true).open()
     │   │    │    ├── apply_easy_options()
     │   │    │    │    ├── easy.url(), .get(), .follow_location()
     │   │    │    │    ├── resume_from() (للـ partial)
     │   │    │    │    ├── range() (للـ segments)
     │   │    │    │    ├── raw_setopt_str() (preProxy, dnsInterface, protocols...)
     │   │    │    │    ├── accept_encoding("") + resume_from → CURL BUG ⚠️
     │   │    │    │    ├── headers (direct_headers)
     │   │    │    │    └── http_version() (HTTP/2, HTTP/3)
     │   │    │    │
     │   │    │    ├── CurlMultiGuard::new()
     │   │    │    ├── add2(easy) → Easy2Handle
     │   │    │    └── loop: drive_multi_*()
     │   │    │         │
     │   │    │         ├── Tick Closure (كل 250ms)
     │   │    │         │    ├── STALL DETECTION BROKEN ⚠️⚠️
     │   │    │         │    ├── update_curl_task_progress()
     │   │    │         │    │    ├── segment_scheduler.update_segment()
     │   │    │         │    │    ├── telemetry_bus.report_bytes/speed
     │   │    │         │    │    ├── AdaptiveEngine.evaluate()
     │   │    │         │    │    │    ├── convergence.should_adjust()
     │   │    │         │    │    │    ├── segment_ctrl.evaluate()
     │   │    │         │    │    │    └── profiler.update_from_telemetry()
     │   │    │         │    │    └── lock_or_err!(state.curl_jobs) ← contention
     │   │    │         │    └── lock_or_err!(state.engine_trackers)
     │   │    │         │
     │   │    │         ├── multi.messages() → collect_multi_errors()
     │   │    │         └── cancel check (AtomicBool)
     │   │    │
     │   │    └── Response Code Handler
     │   │         ├── 200 → OK (أو خطأ مع resume/multi-segment)
     │   │         ├── 206 → OK (range fulfilled)
     │   │         ├── 304 → Conditional success (يحتاج بيانات محلية)
     │   │         ├── 412 → resource-changed-412
     │   │         ├── 416 → range-not-satisfiable-416
     │   │         ├── 429/503 → NOT TRANSIENT ⚠️ (retry لا يعمل)
     │   │         └── >=400 → "HTTP error N"
     │   │
     │   ├── Retry Loop
     │   │    ├── MAX_RETRY_WALL_TIME = 24 ساعة
     │   │    ├── Self-healer ← never called ⚠️
     │   │    ├── Mirror failover
     │   │    ├── Segmented → single fallback
     │   │    └── Exponential backoff + jitter (sleep 500ms chunks)
     │   │
     │   └── Post-loop: extra single attempt if segmented ⚠️
     │
     └── Watchdog Thread ─────────────────────────
          ├── Polls progress كل 3 ثوانٍ
          ├── Hard deadline: max(300, min(10800, size/10000+120))
          └── Force-cancels on stall
```

---

## 4. خريطة الاعتماد بين الوحدات

```
                    ┌──────────────────────────────────────────┐
                    │              lib.rs (Tauri)              │
                    │         starts daemon thread             │
                    └────────────────────┬─────────────────────┘
                                         │
                    ┌────────────────────▼─────────────────────┐
                    │           daemon/mod.rs (main)           │
                    │     initializes all subsystems           │
                    └──┬────┬────┬────┬────┬────┬────┬────┬────┘
                       │    │    │    │    │    │    │    │
          ┌────────────┘    │    │    │    │    │    │    └────────────┐
          ▼                 ▼    ▼    ▼    ▼    ▼    ▼                 ▼
  ┌──────────────┐  ┌───────────┐  ┌──────────────────┐     ┌────────────────┐
  │  routes/     │  │  curl/    │  │  engine/         │     │  external_tools│
  │  extension   │  │  transfer │  │  adaptive/       │     │  installer     │
  │  engine      │──│  task_api │──│  adaptive_conn    │     │  registry      │
  │  (mod.rs)    │  │  easy_cfg │  │  bandwidth       │     └────────────────┘
  └──────┬───────┘  │  multi    │  │  config          │
         │          │  args     │  │  profiles        │
         │          │  direct   │  │  rules           │
         │          └────┬──────┘  │  retry           │
         │               │         │  scheduler       │
         ▼               ▼         │  event_bus       │
  ┌────────────┐  ┌──────────┐     │  checksum        │
  │  state.rs  │◄─│  utils   │◄────│  resource_mgr    │
  │  persist   │  └──────────┘     │  thread_pool     │
  │  mod.rs    │                   │  mirror          │
  └────────────┘                   │  ...             │
                                   └──────────────────┘

أهم التبعيات المخفية:
  • engine/ → curl/transfer.rs (عبر TaskEngineTracker)
  • curl/transfer.rs → engine/adaptive/ (عبر AdaptiveEngine)
  • routes/extension.rs → utils/ (عبر is_safe_target_url)
  • lib.rs → daemon/mod.rs (عبر start_daemon)
```

---

## 5. جميع الأخطاء — مرتبة حسب الخطورة

### 🔴 CRITICAL (2)

| # | الملف | الدالة | السطور | المشكلة | الحل |
|---|-------|--------|--------|---------|------|
| C1 | `transfer.rs` | tick closure | 737-744 | **استقصاء التوقف لا يعمل**: `effective_downloaded > on_disk_before` تقارن بقيمة ثابتة بدلاً من `last_total`. بعد أول بايت، `last_progress_time` تُحدث كل tick إلى الأبد — لا يتم كشف التوقف أبداً | تغيير `on_disk_before` إلى `last_total` |
| C2 | `args.rs` | `apply_raw_curl_options` | 204-224 | **حقن وسائط curl**: `args.extend(parts)` يضيف ALL shell-split parts وليس فقط الخيار المسموح. مهاجم يتحكم بـ `rawOptions` يمكنه إضافة أي وسيط curl (مثل `--output /etc/passwd`) | إضافة الخيار المُتحقق فقط، وليس كل الأجزاء |

### 🟠 HIGH (12)

| # | الملف | الدالة | السطور | المشكلة | الحل |
|---|-------|--------|--------|---------|------|
| H1 | `utils.rs` | `is_safe_target_url` | 32-78 | **DNS rebinding**: الفحص يحل hostname ويتحقق من IP، لكن المتصل يستخدم hostname الأصلي وليس IP المُتحقق — DNS يمكن أن يتغير بعد الفحص | إرجاع IP المُتحقق واستخدامه مباشرة (ـresolve) |
| H2 | `utils.rs` | `is_safe_resolve_entry` | 130-171 | **DNS rebinding**: نفس المشكلة في validate_proxy_url | نفس الحل |
| H3 | `utils.rs` | 모든 دالة SSRF | 630-671 | **DNS rebinding**: validate_proxy_url نفس الثغرة | نفس الحل |
| H4 | `mod.rs` | `start_daemon` | 142-478 | **فشل بدء الديمون غير مرئي**: start_daemon يعمل في thread منفصل ولا يبلغ caller بالنجاح/الفشل. caller يستمر وكأن الديمون بدأ بنجاح | استخدام oneshot channel لإعلام caller |
| H5 | `mod.rs` | `start_daemon` | 290,325,327 | **مهام async يتيمة**: JoinHandles من tokio::spawn يتم تجاهلها — لا تُلغى عند الإيقاف، runtime يُسقط فجأة | جمع JoinHandles وإلغائها مع CancellationToken |
| H6 | `lib.rs` | `restart_daemon` | 466-494 | **قتل عمليات غير مرتبطة**: kill_old_daemon_range يقتل أي process يستمع على نطاق المنافذ، قد يقتل خدمات أخرى | تخزين PID القديم في ملف المنفذ وقتل PID محدد فقط |
| H7 | `extension.rs` | `handle_v1_analyze_progress` | 1128 | **تسريب عملية yt-dlp**: قطع اتصال SSE لا يلغي `spawn_blocking` — عملية yt-dlp تستمر 30 ثانية | استخدام CancellationToken + tokio::select |
| H8 | `task_api.rs` | `pause_task` | 130-146 | **SIGKILL يقطع yt-dlp**: `taskkill /F` يقتل yt-dlp فجأة — ffmpeg subprocess يصبح orphan، ملفات .part تبقى فاسدة | إرسال SIGTERM أولاً ثم الانتظار |
| H9 | `task_api.rs` | `redownload_task` | 410-445 | **سباق حذف الملف مع worker نشط**: cancel_token يُضبط ثم يُحذف الملف فوراً — worker قد يكتب في نفس اللحظة | انتظار worker للاعتراف بالإلغاء قبل حذف الملف |
| H10 | `profile_store.rs` | `PersistedProfile` | 11-33 | **لا يوجد versioning للـ schema**: تغيير الحقول يؤدي إلى فشل serde صامت أو بيانات تالفة | إضافة `schema_version: u32` مع migration logic |
| H11 | `self_healing.rs` | `on_failure` | الكل | **self_healer لا يُستدعى أبداً**: النظام كله wired up لكن لا شيء يستدعيه — إجراءات الاسترداد ميتة | ربطه بمسار الأخطاء في transfer.rs |
| H12 | `policy_engine.rs` | جميع `decide_*` | الكل | **policy_engine لا يُستدعى أبداً**: كل قرارات السياسة (connections, segments, retry, etc.) غير مستخدمة | إما دمجها مع AdaptiveEngine أو إزالتها |

### 🟡 MEDIUM (28)

| # | الملف | الدالة | السطور | المشكلة |
|---|-------|--------|--------|---------|
| M1 | `transfer.rs` | tick closure (multi-segment) | 1109-1217 | لا يوجد استقصاء توقف في مسار multi-segment |
| M2 | `transfer.rs` | `run_libcurl_download` | 1669-1693 | محاولة إضافية بعد استنفاد retries (تتجاوز ال policy) |
| M3 | `transfer.rs` | `run_segmented_libcurl` | 1255-1257 | 200 مع single segment لا يُعتبر خطأ — إمكانية فساد الملف |
| M4 | `transfer.rs` | `is_transient_error` | 395-423 | HTTP 429/503/502 لا تعتبر transient — retry لا يعمل |
| M5 | `transfer.rs` | عديد | 837,849,... | أخطاء بدون سياق — "HTTP error 412" بدون URL أو مسار |
| M6 | `transfer.rs` | streaming hasher | 170-173 | SHA-256 التراكمي لكل segment — التجزئة من آخر segment فقط، خاطئ لـ multi-segment |
| M7 | `transfer.rs` | `remove_on_error` | 259,1771-1775 | مصدران مختلفان لنفس الإعداد — تناقض محتمل |
| M8 | `transfer.rs` | watchdog deadline | 1917-1922 | حد زمني يفترض سرعة لا تقل عن 78Kbps — اتصالات بطيئة تُلغى |
| M9 | `transfer.rs` | tick closure | 500-557,723,1109 | tick يعطل loop التحميل إذا كان curl_jobs lock محتجزاً من API thread |
| M10 | `state.rs` | `AppState` | 44-92 | لا توثيق لترتيب أقفال 12+ Mutex — خطر deadlock كامن |
| M11 | `state.rs` | `engine_trackers` | 68 | Mutex واحد لكل الـ trackers — تنافس تحت الحمل |
| M12 | `persist.rs` | `save` | 106-134 | تسريب ملف tmp على Windows إذا فشل rename |
| M13 | `persist.rs` | `PersistedState.version` | 14-26,85 | version مكتوب لكنه لا يُقرأ أبداً — لا ترحيل |
| M14 | `mod.rs` | `start_daemon` | 418-467 | سباق في بدء إشارة الإيقاف — قد تُفقد إشارة بين بدء الاستماع وأول poll |
| M15 | `mod.rs` | `start_daemon` | 276-279 | discover_and_initialize() يستدعى داخل tokio runtime بدون spawn_blocking |
| M16 | `mod.rs` | `resolve_engine_binary` | 113-140 | **احتمالية هجوم supply-chain**: العودة إلى PATH بدون تحقق |
| M17 | `mod.rs` | auth_middleware | 96-108 | **تسريب API token**: token في query parameter لـ SSE |
| M18 | `lib.rs` | `check_tcp_endpoint` | 344-372 | إنشاء thread غير محدود — لا pool ولا bounds |
| M19 | `extension.rs` | `handle_v1_analyze_progress` | 1121 | `http_probe_for_analyze` يستمر بعد قطع اتصال SSE |
| M20 | `extension.rs` | `http_probe_for_analyze` | 1146-1176 | `Accept-Ranges: Bytes` مقارنة case-sensitive — RFC يقول case-insensitive |
| M21 | `common.rs` | `hidden_output_timed` | 57-59 | فشل `child.kill()` قد يترك orphan/zombie |
| M22 | `common.rs` | `split_cd_params` | 179-207 | انتهاك RFC 6266 — معالجة escape غير صحيحة، quotes غير مغلقة |
| M23 | `common.rs` | `content_disposition_filename` | 240-306 | `filename*=` يفشل دون fallback إلى `filename=` — اسم ملف مفقود |
| M24 | `common.rs` | `percent_decode_str` | 308-328 | UTF-8 غير صالح يُرجع الإدخال الأصلي بدلاً من lossy |
| M25 | `task_api.rs` | `delete_task` | 478-513 | **Ghost task**: job يُحذف من curl_jobs قبل task_snapshot — snapshot يبقى |
| M26 | `task_api.rs` | `sanitize_new_name` | 229-246 | Unicode normalization غير محمي — traversal عبر `..` متشابهة |
| M27 | `easy_config.rs` | `reject_unsafe_protocols` | 1189-1205 | قائمة FORBIDDEN غير كاملة — tftp, ldaps, imap, smtp مفقودين |
| M28 | `resource_monitor.rs` | `estimate_cpu_usage_fallback` | 217-220 | CPU fallback = `disk_write_mbps / 100.0` — لا معنى له |

### 🔵 LOW (34)

| # | الملف | المشكلة |
|---|-------|---------|
| L1 | `transfer.rs` | سباق إكمال vs إلغاء (نافذة ضيقة جداً) |
| L2 | `transfer.rs` | `resume_existing` غير متناسق مع curl resume |
| L3 | `transfer.rs` | ملفات temp من merge فاشل لا تُنظف |
| L4 | `transfer.rs` | `multi()` بعد into_inner() — panic محتمل (fix applied) |
| L5 | `state.rs` | Thundering herd على cache recomputation |
| L6 | `persist.rs` | TOCTOU في has_active polling interval |
| L7 | `lib.rs` | `open_external_url` يسمح بـ http:// (MITM) |
| L8 | `lib.rs` | مسح منافذ unbounded (16383 محاولة ربط) |
| L9 | `lib.rs` | `validate_file_path` يتطلب وجود الملف — لا يعمل للتحميلات الجديدة |
| L10 | `utils.rs` | بقع IP bogon مفقودة (192.0.2.0/24 وغيرها) |
| L11 | `utils.rs` | Quotes غير مغلقة في shell_split |
| L12 | `utils.rs` | kill_process يتجاهل أخطاء taskkill |
| L13 | `utils.rs` | Mirror URLs من HTTP headers بدون تحقق SSRF |
| L14 | `utils.rs` | افتراض case-insensitive لاسم header هش |
| L15 | `utils.rs` | متغير `shutdown_requested` يحتاج تدقيق Atomic Ordering |
| L16 | `utils.rs` | `lock_or_err!` يسمح بـ poison recovery بدون تحقق — تلف بيانات صامت محتمل |
| L17 | `extension.rs` | `dedup_by` يزيل التكرارات المتتالية فقط — تكرارات غير متتالية تبقى |
| L18 | `extension.rs` | لا يوجد endpoint GET /v1/tasks/{id} |
| L19 | `extension.rs` | `task_id_from_json` يصمت عن `id` القديم — لا deprecation warning |
| L20 | `extension.rs` | سباق على تكرار `formats` — لكن serde_json immutable OK |
| L21 | `task_api.rs` | `ensure_parent` يُستدعى قبل التحقق من args |
| L22 | `task_api.rs` | `pause_task` يرجع "paused" قبل أن يتوقف worker فعلاً |
| L23 | `task_api.rs` | `update_task_metadata` يسمح بتغيير URL لـ completed task |
| L24 | `task_api.rs` | `delete_task` غير idempotent — الاستدعاء الثاني يعيد error |
| L25 | `easy_config.rs` | `raw_setopt_str` — مؤشر easy_ptr يأتي من `easy.raw()` وهو صالح |
| L26 | `easy_config.rs` | "3-only" → `HttpVersion::V3` وليس V3Only لا فرق بين "3" و "3-only" |
| L27 | `easy_config.rs` | Bearer token قد يظهر في رسالة الخطأ |
| L28 | `easy_config.rs` | tls_backend_needs_explicit_ca لا يكتشف BearSSL/mbedTLS/wolfSSL |
| L29 | `easy_config.rs` | `installed_ca_bundle_path()` يُستدعى مرتين |
| L30 | `easy_config.rs` | `etagCompare` بدون validate path — قراءة ملفات عشوائية |
| L31 | `direct.rs` | ملف temp من merge لا يُنظف في Drop |
| L32 | `direct.rs` | `delay_for_attempt` انتروبيا jitter ضعيفة |
| L33 | `resource_monitor.rs` | `record_disk_write()` لم يُستدعى أبداً — disk_bottleneck ميت |
| L34 | `retry.rs` | jitter يستخدم `subsec_nanos()` — thundering herd محتمل |

---

## 6. الأكواد الميتة

| الوحدة | الملف | السطور | الحالة | % ميت |
|--------|-------|--------|--------|-------|
| **disk_writer.rs** | `engine/adaptive/` | 355 | **DEAD** — AsyncDiskWriter مبني فقط في الاختبارات | 100% |
| **self_healing.rs** | `engine/` | ~200 | **DEAD** — on_failure() لا يُستدعى أبداً من production | ~90% |
| **policy_engine.rs** | `engine/` | ~500 | **DEAD** — جميع decide_* لا تُستدعى أبداً | ~95% |
| **plugin_api.rs** | `engine/` | ~170 | **DEAD (skeleton)** — لا runtime ولا plugin loading | ~60% |
| **AdaptationAction::ThrottleAll** | `engine/adaptive/mod.rs` | سطر 305 | DEAD variant — غير مستخدم | 1 variant |
| **Streaming SHA-256** | `easy_config.rs` | 170-196 | DEAD في multi-segment — التجزئة من كل segment تُلغى بآخر segment | غير فعال |
| **BufferManager recommendations** | `engine/adaptive/buffer_manager.rs` | الكل | DEAD — AsyncDiskWriter يقرأ من config فقط وليس من recommed() | غير فعال |
| **resource_monitor::record_disk_write** | `resource_monitor.rs` | 293-295 | DEAD — لا يُستدعى أبداً | 1 function |
| **adaptive_connections::should_adjust** | `engine/adaptive_connections.rs` | fallback | DE facto dead — AdaptiveEngine موجود دائماً | fallback path |
| **chunk_manager.rs** | `engine/` | 297 | DEAD — import فقط في adaptive/mod.rs حيث هو dead أيضاً | 100% |
| **die_orchestrator.rs** | `engine/` | 231 | DEACTIVE — مبني في state لكن دوره غير واضح في التحميل الفعلي | ~70% |

**إجمالي الكود الميت المقدر: ~1,200 سطر (8% من الكود)**

---

## 7. مشاكل التزامن

| # | المشكلة | الخطورة | الموقع |
|---|---------|---------|--------|
| SY1 | **ترتيب الأقفال غير موثق**: 15+ Mutex في AppState بدون ترتيب محدد — ABBA deadlock متوقع | HIGH | `state.rs` |
| SY2 | **Spurious wakeup في drive_multi_socket**: if `ready_count > 0 && dispatched == 0` → else branch ينفذ action على كل socket — قد يُسبب duplicate action | MEDIUM | `multi.rs:439-455` |
| SY3 | **استقصاء التوقف (stall detection) معطل**: يقارن بقيمة ثابتة — لا يكشف stall أبداً | CRITICAL | `transfer.rs:737-744` |
| SY4 | **لا استقصاء توقف في multi-segment**: tick لا يتحقق من stall إطلاقاً | MEDIUM | `transfer.rs:1109-1217` |
| SY5 | **إلغاء vs إكمال — سباق**: Ok path لا يتحقق من cancel بعد عودة drive loop | LOW | `transfer.rs:1417-1481` |
| SY6 | **Mutex poison recovery**: `lock_or_err!` يستعيد من poison بدون إعادة بناء الحالة — تلف بيانات محتمل | MEDIUM | `utils.rs:9-28` |
| SY7 | **engine_trackers lock contention**: Mutex واحد لكل trackers — API threads و worker threads يتنافسون | MEDIUM | `state.rs:68` |
| SY8 | **Tick blocking**: curl_jobs lock في tick يعطل loop التحميل | MEDIUM | `transfer.rs` tick closures |
| SY9 | **Ghost task في delete**: job يُحذف قبل snapshot — snapshot يبقى | MEDIUM | `task_api.rs:478-513` |
| SY10 | **سباق حذف ملف مع worker نشط**: cancel يُضبط ثم الملف يُحذف فوراً | HIGH | `task_api.rs:410-445` |
| SY11 | **شروع إشارة الإيقاف**: قد تُفقد إشارة قبل أن يبدأ select | MEDIUM | `mod.rs:418-467` |

---

## 8. مشاكل libcurl

| # | المشكلة | الخطورة | الموقع |
|---|---------|---------|--------|
| L1 | **resume_from + accept_encoding**: تفاعل غير معروف — قد يُفسد الملف عند الاستئناف مع الضغط | MEDIUM | `easy_config.rs:395-398,438-441` |
| L2 | **HTTP/2 → HTTP/3Only**: لا فرق بين "http3" و "http3-only" في curl crate — V3Only غير معرّف | LOW | `easy_config.rs:877-881` |
| L3 | **قائمة FORBIDDEN غير كاملة**: tftp, gophers, imap/s, smtp/s مفقودة | MEDIUM | `easy_config.rs:1189-1205` |
| L4 | **CURLOPT_PROTOCOLS_STR يحتاج curl >= 7.85.0**: خطأ صامت مع curl قديم | LOW | `easy_config.rs:499-509` |
| L5 | **raw_setopt_str**: `CString::new(value)` يتحقق من NUL bytes — صحيح | ✅ OK | `easy_config.rs:29-42` |
| L6 | **multi.messages()**: iterates ALL handles per message O(n*m) | LOW | `multi.rs:303-321` |
| L7 | **Socket reassignment token**: token قد يصل إلى usize::MAX ويلتف — لكن libcurl يتعامل معه | LOW | `multi.rs:238-248` |
| L8 | **TLS backend detection**: ينقصه BearSSL, mbedTLS, wolfSSL — خطأ صامت مع هذه backends | MEDIUM | `easy_config.rs:266-279` |

---

## 9. مشاكل البنية

| # | المشكلة | الخطورة | الموقع |
|---|---------|---------|--------|
| A1 | **نظامان تكيف متوازيان**: AdaptiveEngine + AdaptiveConnectionManager — duplicate logic | MEDIUM | `engine/adaptive/mod.rs` + `adaptive_connections.rs` |
| A2 | **engine/mod.rs يصرّح بـ chunk_manager + die_orchestrator**: كلاهما dead | MEDIUM | `engine/mod.rs:5,7` |
| A3 | **extractor.rs + plugin_api.rs**: extractor يعمل، plugin_api مجرد skeleton | LOW | `engine/extractor.rs` + `plugin_api.rs` |
| A4 | **كود ميت ~1,200 سطر**: disk_writer, policy_engine, self_healing, plugin_api | HIGH | متعدد |
| A5 | **لا تغطية اختبارات لنظام كامل**: اختبارات فردية لكل وحدة لكن لا integration tests | MEDIUM | جميع |
| A6 | **تكرار كود TLS version parsing**: 3 blocks متطابقة في easy_config.rs | LOW | `easy_config.rs:700-712,894-906,908-923` |
| A7 | **`profile_store.rs` لا schema versioning**: تغيير الحقل → تلف صامت | HIGH | `profile_store.rs:11-33` |
| A8 | **`common.rs` Content-Disposition parser**: انتهاك RFC 6266 في 3 مواضع | MEDIUM | `common.rs:179-346` |

---

## 10. نقاط ضعف الأداء

| # | المشكلة | التأثير | الموقع |
|---|---------|---------|--------|
| P1 | **mutex lock في tick (curl_jobs, engine_trackers)**: يعطل loop التحميل إذا كان API thread يحتجز lock | من 10ms إلى عدة ثوانٍ | `transfer.rs` tick closures |
| P2 | **engine_trackers Mutex واحد**: API handlers و worker threads يتنافسون | يتناسب مع عدد API requests + downloads | `state.rs:68` |
| P3 | **bandwidth.rs O(n) eviction**: يمسح 5000 إدخال عند كل unique task جديد | ~5000 مقارنة لكل task | `bandwidth.rs:131-138` |
| P4 | **server_profiler O(n log n)**: sort 100 RTT samples كل telemetry update | ~700 مقارنة كل tick | `server_profiler.rs:148-176` |
| P5 | **profile_store يحفظ تحت lock**: `save()` يعمل I/O على القرص (ms) بينما يمسك Mutex — يمنع كل عمليات profiles | ~100ms+ | `profile_store.rs:328-342` |
| P6 | **Busy-wait في hidden_output_timed**: 50ms sleep مع try_wait لكل process — 20 عملية = 400 wake/s | ~20% CPU تحت الحمل | `common.rs:39-72` |
| P7 | **Sleep في multi_wait_perform**: 250ms sleep مع wait(&[]) — inefficient vs socket mode | ~250ms latency | `multi.rs:354-356` |
| P8 | **find_available_daemon_port loop unbounded**: حتى 16383 محاولة ربط | ثوانٍ من التأخير | `lib.rs:113-124` |

---

## 11. مشاكل أمنية

| # | المشكلة | CWE | الخطورة | الموقع |
|---|---------|-----|---------|--------|
| S1 | **حقن وسائط curl** عبر rawOptions | CWE-88 | CRITICAL | `args.rs:204-224` |
| S2 | **DNS rebinding** — SSRF | CWE-346 | HIGH | `utils.rs:32-78,130-171,630-671` |
| S3 | **PATH fallback** — supply chain | CWE-426 | HIGH | `mod.rs:113-140` |
| S4 | **لا توثيق API** — أي process محلي يمكنه استدعاء API | CWE-306 | HIGH | `routes/mod.rs` |
| S5 | **SSE token في query parameter** — تسرب | CWE-598 | MEDIUM | `mod.rs:96-108` |
| S6 | **etagCompare بدون path validation** — قراءة ملفات عشوائية | CWE-22 | MEDIUM | `easy_config.rs:1132-1141` |
| S7 | **save_path بدون traversal validation** | CWE-22 | MEDIUM | `args.rs:246-258` |
| S8 | **Mirror URLs من HTTP headers بدون SSRF check** | CWE-346 | MEDIUM | `utils.rs:467-512` |
| S9 | **sanitize_new_name — Unicode traversal** | CWE-22 | MEDIUM | `task_api.rs:229-246` |
| S10 | **QA**. كود بدون أي تحقق أمني | CWE-20 | MEDIUM | `extension.rs` yt-dlp calls |

---

## 12. خطة الإصلاح

### المرحلة 1: حرجة (فوراً)
| الأولوية | المعرف | الجهد | الوصف |
|----------|--------|-------|-------|
| P0 | C1 | 1h | إصلاح استقصاء التوقف: تغيير `on_disk_before` إلى `last_total` |
| P0 | C2 | 2h | إصلاح حقن rawOptions: `args.extend(parts)` ← إضافة الخيار المُتحقق فقط |

### المرحلة 2: عالية (خلال أسبوع)
| الأولوية | المعرف | الجهد | الوصف |
|----------|--------|-------|-------|
| P1 | H1-3 | 4h | حماية DNS rebinding: حجز IP المُتحقق واستخدامه بدلاً من hostname |
| P1 | H4 | 2h | إخطار caller بنجاح/فشل بدء الديمون via oneshot |
| P1 | H5 | 2h | جمع JoinHandles + CancellationToken للإيقاف الآمن |
| P1 | H6 | 1h | تخزين PID القديم وقتل PID محدد فقط |
| P1 | H7 | 3h | CancellationToken لـ SSE + select للكشف عن قطع الاتصال |
| P1 | H8-H9 | 2h | إرسال SIGTERM أولاً + انتظار قبل حذف الملفات |
| P1 | H10 | 1h | إضافة `schema_version` مع migration في profile_store |
| P1 | H11-H12 | 4h | إما ربط self_healer/policy_engine أو إزالتها مع الكود الميت |
| P1 | SY1 | 2h | توثيق ترتيب الأقفال + إضافة runtime check |
| P1 | S4 | 4h | إضافة auth middleware لكل routes |

### المرحلة 3: متوسطة (خلال شهر)
| الأولوية | المعرف | الجهد | الوصف |
|----------|--------|-------|-------|
| P2 | M1-8 | 8h | جميع أخطاء MEDIUM في transfer.rs |
| P2 | M10-17 | 6h | جميع أخطاء MEDIUM في state/mod.rs/lib.rs |
| P2 | M19-28 | 10h | جميع أخطاء MEDIUM في extension/common/task_api/easy_config |
| P2 | A1 | 4h | دمج AdaptiveEngine + AdaptiveConnectionManager |
| P2 | L33 | 1h | ربط record_disk_write بمسار I/O الفعلي |

### المرحلة 4: تحسينات (شهرين)
| الأولوية | المعرف | الجهد | الوصف |
|----------|--------|-------|-------|
| P3 | A2 | 1h | إزالة chunk_manager + die_orchestrator من mod.rs |
| P3 | P1-8 | 8h | جميع تحسينات الأداء (lock contention, O(n), unbounded loops) |
| P3 | L1-34 | 12h | جميع الأخطاء LOW |
| P3 | كود ميت | 4h | إزالة disk_writer, policy_engine, self_healing (إن لم تُربط) |

---

*نهاية التقرير — 76 خطأ مكتشف، 2 حرج، 12 عالي، 28 متوسط، 34 منخفض*
